class Transport {
            public string Make;
            public string Model;
            public Color Colour;

            public Transport (string make, string model, Color colour) {
                Make = make;
                Model = model;
                Colour = colour;
            }
        }
